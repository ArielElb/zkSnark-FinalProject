{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/libprime-selection", // Path to your compiled executable
      "args": [], // If you don't want to pass any command line arguments, leave it empty
      "cwd": "${workspaceFolder}"
    }
  ]
}
